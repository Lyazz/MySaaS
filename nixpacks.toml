[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["rm -f package-lock.json", "pnpm install --no-frozen-lockfile --ignore-scripts"]

[phases.build]
cmds = ["pnpm nuxt prepare", "pnpm run build"]
